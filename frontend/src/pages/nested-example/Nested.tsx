import React from 'react';
import Page from '../../components/Page';

const Nested = () => {
    return (
        <Page>
            <div>Nested</div>
        </Page>
    );
};

export default Nested;